---
phase: 01-foundation-branded-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/HeroSection.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-04

must_haves:
  truths:
    - "User sees '¿Cuánto vale tu casa?' headline with 'tu casa' in electric blue (text-primary)"
    - "User sees an orange CTA button labeled 'Valorar mi propiedad' using .btn-primary class"
    - "User sees a subtitle explaining the tool's purpose in Spanish"
    - "User sees a styled placeholder card with surface-tint background, rounded corners, and a Lucide icon"
    - "All visible text on the page is in Spanish"
    - "Page layout is single-column scroll with no navigation header"
  artifacts:
    - path: "frontend/src/components/HeroSection.tsx"
      provides: "PropHero-branded hero section with headline, subtitle, and CTA"
      exports: ["HeroSection"]
    - path: "frontend/src/App.tsx"
      provides: "Layout shell rendering HeroSection and PlaceholderCard"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/HeroSection.tsx"
      via: "named import and JSX render"
      pattern: "import.*HeroSection.*from"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/main.tsx"
      via: "default export consumed by createRoot render"
      pattern: "export default"
    - from: "frontend/src/components/HeroSection.tsx"
      to: "frontend/src/index.css"
      via: ".btn-primary CSS class and h1/h2 typography rules"
      pattern: "btn-primary"
---

<objective>
Create the PropHero-branded hero section component and rewrite the App layout shell to display the landing page with hero content and a placeholder card for the form area.

Purpose: Deliver the visual shell that users see when they load the page — a professional, trust-building PropHero brand experience with clear Spanish copy and a styled mount point for downstream form components.
Output: Working branded landing page with hero headline, subtitle, CTA button, and placeholder card.
</objective>

<execution_context>
@/Users/prophero/development/how-much-is-this-property-worth/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/prophero/development/how-much-is-this-property-worth/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-branded-shell/01-CONTEXT.md
@.planning/phases/01-foundation-branded-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-branded-shell/01-PATTERNS.md

<interfaces>
<!-- Existing utilities and patterns the executor needs -->

From frontend/src/lib/utils.ts:
- export function cn(...inputs: ClassValue[]): string
- Import as: import { cn } from '@/lib/utils'

From frontend/src/main.tsx:
- import App from './App' (default import — App.tsx MUST maintain default export)
- Renders <StrictMode><App /></StrictMode>

CSS classes available (from frontend/src/index.css @layer components):
- .btn-primary: orange bg (#f45504), white text, pill radius, shadow-card, hover:bg-accent-light, focus ring
- .btn-secondary: blue border, white bg, pill radius
- .btn-ghost: transparent, subtle hover

Tailwind tokens (from frontend/tailwind.config.js):
- Colors: bg-surface (#fff), bg-surface-tint (#f3f5fe), bg-surface-muted (#f5f7f9), text-primary (#2050f6), text-ink (#1e252d), text-ink-secondary (#596b7d), text-ink-muted (#abb8c7), bg-accent (#f45504)
- Spacing: xs(4px), sm(8px), md(16px), lg(24px), xl(32px), 2xl(48px), 3xl(64px)
- Radius: rounded-pill (9999px), rounded-2xl (20px), rounded-xl (16px)
- Shadows: shadow-card (0 10px 40px rgba(32,80,246,0.08)), shadow-lift

Typography (from frontend/src/index.css):
- h1: clamp(2rem, 4vw+1rem, 3.25rem), weight 600, line-height 1.1, letter-spacing -0.03em, margin 24px 0 12px
- Body: 18px/1.45 desktop, 16px on ≤1024px

Lucide icons (installed, from lucide-react):
- Import as: import { Home } from 'lucide-react' (or Building2, MapPin, etc.)

Component export convention (from frontend/src/components/ui/button.tsx):
- Use @/lib/utils import path
- Named function exports for app-level components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection presentational component</name>
  <files>frontend/src/components/HeroSection.tsx (create)</files>
  <read_first>
    frontend/src/index.css,
    frontend/src/components/ui/button.tsx,
    frontend/tailwind.config.js,
    frontend/src/lib/utils.ts,
    .planning/phases/01-foundation-branded-shell/01-UI-SPEC.md
  </read_first>
  <action>
    Create src/components/HeroSection.tsx as a stateless presentational component with no props (per D-01, D-02, BRAND-01).

    The component renders a section element with white background (bg-surface), containing:

    1. An h1 headline with two visual lines: "¿Cuánto vale" in default ink color (text-ink, handled by existing h1 CSS rule for color: var(--text-h)), followed by a span wrapping "tu casa" with className text-primary (electric blue #2050f6) and a question mark after the span. The existing h1 CSS rule in index.css handles font-size (clamp), weight (600), line-height (1.1), letter-spacing (-0.03em), and margin (24px 0 12px).

    2. A subtitle paragraph with className text-ink-secondary, max-width 560px, and margin-top 12px (mt-3). Copy: "Introduce los datos de tu propiedad y te mostramos comparables reales para estimar su valor de mercado." (from UI-SPEC copywriting contract).

    3. A CTA anchor element with className btn-primary, margin-top 24px (mt-6), display inline-flex. The href should be "#form-area" (scroll-to-placeholder behavior per RESEARCH.md recommendation). Label: "Valorar mi propiedad" (from UI-SPEC copywriting contract).

    Section padding: pt-2xl (48px) top, pb-xl (32px) bottom, px-xl (32px) horizontal on desktop, px-md (16px) on mobile (use responsive prefix md:px-xl with base px-md). All text left-aligned (default, no text-center).

    Use named function export: `export function HeroSection()`. Import cn from @/lib/utils only if conditional classes are needed. No React import needed (JSX transform handles it). No props, no state, no effects — pure markup per D-11.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "¿Cuánto vale" src/components/HeroSection.tsx && grep -q "text-primary" src/components/HeroSection.tsx && grep -q "btn-primary" src/components/HeroSection.tsx && grep -q "Valorar mi propiedad" src/components/HeroSection.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - HeroSection.tsx exists at src/components/HeroSection.tsx
    - Exports a named function HeroSection (not default export)
    - Contains h1 with text "¿Cuánto vale" and a span with className containing "text-primary" wrapping "tu casa"
    - Contains paragraph with text "Introduce los datos de tu propiedad y te mostramos comparables reales para estimar su valor de mercado."
    - Paragraph has className containing text-ink-secondary and max-w constraint
    - Contains anchor or button with className containing btn-primary and text "Valorar mi propiedad"
    - CTA has href="#form-area"
    - Section has padding classes using spacing tokens (pt-2xl, pb-xl, px-md with md:px-xl breakpoint)
    - No useState, useEffect, or any React hooks used
    - No props accepted by the function
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>HeroSection component renders PropHero-branded hero with Spanish headline, subtitle, and orange CTA button, all left-aligned</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite App.tsx as layout shell</name>
  <files>frontend/src/App.tsx (rewrite)</files>
  <read_first>
    frontend/src/App.tsx,
    frontend/src/main.tsx,
    frontend/src/components/HeroSection.tsx,
    .planning/phases/01-foundation-branded-shell/01-UI-SPEC.md
  </read_first>
  <action>
    Rewrite src/App.tsx from scratch — remove ALL existing demo content (TabBarLayout, counter, logos, demo sections per D-05). Start with a clean file.

    Import HeroSection from @/components/HeroSection. Import the Home icon (or Building2) from lucide-react for the placeholder card.

    Create a function App() that returns a fragment or single wrapper containing:

    1. The HeroSection component rendered at the top.

    2. A placeholder card section below the hero (per D-06, D-07), implemented inline (not a separate component). This is a div with id="form-area" (CTA scroll target), styled with: bg-surface-tint (#f3f5fe), rounded-2xl (20px radius), shadow-card, padding p-2xl (48px), min-height 200px, margin-top mt-xl (32px gap from hero), horizontal margin mx-xl desktop / mx-md mobile (use responsive: mx-md md:mx-xl), margin-bottom mb-3xl (64px bottom breathing room). Content centered using flex column items-center justify-center with gap. Contains a Lucide icon (Home or Building2, size 48, className text-ink-muted with opacity-50), a heading "Aquí aparecerá el formulario de valoración" in text-ink-muted font-medium, and a subtext paragraph "Completa los datos de tu propiedad para obtener una estimación basada en comparables del mercado." in text-ink-muted text-sm with max-width for readability. (All copy from UI-SPEC copywriting contract).

    Export App as default export (required — main.tsx imports it as `import App from './App'`).

    Do NOT import App.css (deleted), TabBar, react.svg, vite.svg, hero.png, or any demo assets. Do NOT use useState or any hooks — this is a static layout shell. No navigation header (per D-04).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "HeroSection" src/App.tsx && grep -q "form-area" src/App.tsx && grep -q "surface-tint" src/App.tsx && grep -q "export default" src/App.tsx && ! grep -q "TabBar\|App\.css\|react\.svg\|useState" src/App.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx imports HeroSection from @/components/HeroSection
    - App.tsx imports a Lucide icon from lucide-react
    - App.tsx renders HeroSection as the first child element
    - App.tsx contains a div with id="form-area" (placeholder card / CTA scroll target)
    - Placeholder card has className containing bg-surface-tint, rounded-2xl, shadow-card, min-h (200px minimum)
    - Placeholder card contains text "Aquí aparecerá el formulario de valoración" and the subtext
    - App.tsx has `export default App` (default export for main.tsx compatibility)
    - App.tsx does NOT import TabBar, App.css, react.svg, vite.svg, hero.png, or useState
    - No navigation header rendered (D-04)
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds with no errors
  </acceptance_criteria>
  <done>App.tsx is a clean layout shell rendering HeroSection and a styled placeholder card, with no demo content, no navigation, and a default export</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none active) | Phase 1 UI is entirely static — no user input, no API calls, no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-01-04 | Tampering (XSS) | HeroSection.tsx, App.tsx | accept | All content is static JSX strings. React auto-escapes expressions. No dangerouslySetInnerHTML, no user-supplied data rendered. Zero XSS surface. |
| T-01-05 | Spoofing | CTA anchor href | accept | href="#form-area" is a same-page anchor. No external navigation, no phishing risk. |
</threat_model>

<verification>
- `npm run build` completes without errors
- `npx tsc --noEmit` passes
- Page renders HeroSection with Spanish headline and orange CTA
- Placeholder card is visible below hero with surface-tint background
- No demo content visible (no React/Vite logos, no counter, no English demo text)
- No console errors on page load
</verification>

<success_criteria>
User loads the page and sees a PropHero-branded landing page with "¿Cuánto vale tu casa?" headline (blue accent), Spanish subtitle, orange CTA button, and a styled placeholder card — matching the UI-SPEC layout contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-branded-shell/01-02-SUMMARY.md`
</output>
