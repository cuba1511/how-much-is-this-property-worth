---
phase: 01-foundation-branded-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend2/index.html
  - frontend2/src/index.css
  - frontend2/src/App.css
  - frontend2/src/assets/react.svg
  - frontend2/src/assets/vite.svg
  - frontend2/src/assets/hero.png
  - frontend2/src/components/TabBar.tsx
  - frontend2/src/components/ui/tabs.tsx
  - frontend2/src/lib/types.ts
  - frontend2/src/lib/schemas.ts
  - frontend2/src/lib/api.ts
  - frontend2/package.json
  - frontend2/.env.example
autonomous: true
requirements:
  - BRAND-02
  - BRAND-04

must_haves:
  truths:
    - "HTML document declares lang='es' and page title is in Spanish"
    - "Page root container has left-aligned text (text-align: center removed)"
    - "TypeScript interfaces in lib/types.ts mirror every backend Pydantic model field-by-field"
    - "Zod schema validates ValuationRequest with frontend UX bounds (m2: 20-500, bedrooms: 1-10, bathrooms: 1-5)"
    - "API client targets POST /api/valuation with typed request and response"
    - "No demo files remain (App.css, react.svg, vite.svg, hero.png, TabBar.tsx, tabs.tsx)"
  artifacts:
    - path: "frontend2/src/lib/types.ts"
      provides: "TypeScript interfaces for full API contract"
      exports: ["ResolvedAddress", "ValuationRequest", "MunicipioInfo", "Listing", "ValuationStats", "MarketTransactionChartPoint", "MarketTransaction", "MarketTransactionsSummary", "MarketTransactions", "SearchStageResult", "SearchMetadata", "ValuationResponse"]
    - path: "frontend2/src/lib/schemas.ts"
      provides: "Zod validation schema for form inputs"
      exports: ["valuationRequestSchema", "ValuationRequestForm"]
    - path: "frontend2/src/lib/api.ts"
      provides: "API client stub"
      exports: ["valuateProperty"]
  key_links:
    - from: "frontend2/src/lib/types.ts"
      to: "backend/models.py"
      via: "manual TypeScript mirror of Pydantic models"
      pattern: "export interface ValuationRequest"
    - from: "frontend2/src/lib/schemas.ts"
      to: "zod"
      via: "import { z } from 'zod'"
      pattern: "z\\.object"
    - from: "frontend2/src/lib/api.ts"
      to: "frontend2/src/lib/types.ts"
      via: "type imports for request/response typing"
      pattern: "import type.*from.*types"
    - from: "frontend2/src/lib/api.ts"
      to: "backend/main.py POST /api/valuation"
      via: "fetch call to backend endpoint"
      pattern: "/api/valuation"
---

<objective>
Clean up the scaffolded demo artifacts, apply Spanish language settings, fix the root layout for left-aligned text, install Zod, and create the three foundation TypeScript modules (types, schemas, API client) that downstream phases consume.

Purpose: Establish the project's code foundation — typed API contract mirroring the backend, validation schema for future form inputs, and a clean project free of demo cruft — so Phases 2-4 can build on solid ground.
Output: Clean project with foundation modules ready for consumption; all demo assets removed.
</objective>

<execution_context>
@/Users/prophero/development/how-much-is-this-property-worth/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/prophero/development/how-much-is-this-property-worth/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-branded-shell/01-CONTEXT.md
@.planning/phases/01-foundation-branded-shell/01-RESEARCH.md
@.planning/phases/01-foundation-branded-shell/01-PATTERNS.md

<interfaces>
<!-- Backend Pydantic models — the source of truth for frontend TypeScript types -->

From backend/models.py:
- ResolvedAddress: label(str), lat(float), lon(float), municipality(str), province(str?), road(str?), house_number(str?), postcode(str?), neighbourhood(str?), quarter(str?), city_district(str?), country(str?), provider(str="nominatim"), provider_id(str?), precision(str?)
- ValuationRequest: address(str), m2(int, gt=0), bedrooms(int, ge=0), bathrooms(int, ge=1), selected_address(ResolvedAddress?)
- MunicipioInfo: name(str), slug(str), province(str?), lat(float?), lon(float?), road(str?), neighbourhood(str?), quarter(str?), city_district(str?), postcode(str?)
- Listing: title(str), price(int?), m2(int?), price_per_m2(int?), bedrooms(int?), bathrooms(int?), address(str?), url(str), image_url(str?), floor(str?), source_stage(str?)
- ValuationStats: total_comparables(int), avg_price(int?), min_price(int?), max_price(int?), avg_price_per_m2(int?), estimated_value(int?), price_range_low(int?), price_range_high(int?)
- MarketTransactionChartPoint: label(str), asking_price(int?), closing_price(int?), negotiation_margin_pct(float?)
- MarketTransaction: id(str), address(str?), m2(int?), bedrooms(int?), bathrooms(int?), asking_price(int?), closing_price(int?), asking_price_per_m2(int?), closing_price_per_m2(int?), negotiation_margin_pct(float?), close_date(str?), days_on_market(int?), source(str), distance_m(int?)
- MarketTransactionsSummary: total_transactions(int), avg_asking_price(int?), avg_closing_price(int?), avg_asking_price_per_m2(int?), avg_closing_price_per_m2(int?), asking_vs_closing_gap_pct(float?), negotiation_margin_pct(float?), sample_size(int), chart_series(MarketTransactionChartPoint[])
- MarketTransactions: summary(MarketTransactionsSummary), transactions(MarketTransaction[])
- SearchStageResult: name(str), label(str), query(str), search_url(str), listings_found(int), duration_ms(int), area_min(int?), area_max(int?), bedrooms_mode(str), bathrooms_mode(str)
- SearchMetadata: strategy(str), target_comparables(int), final_stage(str), total_duration_ms(int), stages(SearchStageResult[])
- ValuationResponse: municipio(MunicipioInfo), listings(Listing[]), stats(ValuationStats), search_url(str), search_metadata(SearchMetadata), market_transactions(MarketTransactions?)

From frontend2/src/lib/utils.ts:
- export function cn(...inputs: ClassValue[]): string

Pydantic → TypeScript mapping:
- str → string
- int → number
- float → number
- Optional[T] = None → T | null (use optional ? on interface fields)
- list[T] → T[]
- Field(default_factory=list) → T[] (just the type, no default in interface)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove demo artifacts and apply global settings</name>
  <files>
    frontend2/src/App.css (delete),
    frontend2/src/assets/react.svg (delete),
    frontend2/src/assets/vite.svg (delete),
    frontend2/src/assets/hero.png (delete),
    frontend2/src/components/TabBar.tsx (delete),
    frontend2/src/components/ui/tabs.tsx (delete),
    frontend2/index.html (modify),
    frontend2/src/index.css (modify)
  </files>
  <read_first>
    frontend2/index.html,
    frontend2/src/index.css,
    frontend2/src/components/TabBar.tsx,
    frontend2/src/components/ui/tabs.tsx
  </read_first>
  <action>
    Delete the six demo files: src/App.css, src/assets/react.svg, src/assets/vite.svg, src/assets/hero.png, src/components/TabBar.tsx, src/components/ui/tabs.tsx (per D-03, UI-SPEC "Assets to Remove").

    Before deleting tabs.tsx, verify no other file imports from @/components/ui/tabs or @radix-ui/react-tabs besides TabBar.tsx. If nothing else imports it, also run `npm uninstall @radix-ui/react-tabs` to remove the unused dependency.

    In frontend2/index.html:
    - Change `lang="en"` to `lang="es"` on the html element (per BRAND-04)
    - Change the title tag content to "¿Cuánto vale tu casa? — PropHero"
    - Preserve all Google Fonts preconnect links and the Inter font stylesheet link (lines 7-12) — these are critical for font loading

    In frontend2/src/index.css:
    - Remove the `text-align: center;` declaration from the `#root` rule block (line 158). Keep all other #root properties (width, max-width, margin, border-inline, min-height, display, flex-direction, box-sizing).
  </action>
  <verify>
    <automated>cd frontend2 && grep -q 'lang="es"' index.html && grep -q "¿Cuánto vale tu casa?" index.html && ! test -f src/App.css && ! test -f src/assets/react.svg && ! test -f src/components/TabBar.tsx && grep -v '^/\*' src/index.css | grep -v '^\s*//' | grep -c 'text-align:\s*center' | grep -q '^0$' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `lang="es"` on the html element
    - index.html title tag contains "¿Cuánto vale tu casa? — PropHero"
    - index.html still contains the Google Fonts stylesheet link for Inter
    - src/App.css does not exist
    - src/assets/react.svg does not exist
    - src/assets/vite.svg does not exist
    - src/assets/hero.png does not exist
    - src/components/TabBar.tsx does not exist
    - src/components/ui/tabs.tsx does not exist
    - src/index.css #root rule has no text-align: center declaration
    - src/index.css #root rule still has width: 1126px, max-width: 100%, margin: 0 auto, min-height: 100svh
  </acceptance_criteria>
  <done>All demo artifacts removed, index.html declares Spanish language with Spanish title, #root CSS is left-aligned</done>
</task>

<task type="auto">
  <name>Task 2: Install Zod and create foundation TypeScript modules</name>
  <files>
    frontend2/package.json (modify via npm install),
    frontend2/src/lib/types.ts (create),
    frontend2/src/lib/schemas.ts (create),
    frontend2/src/lib/api.ts (create),
    frontend2/.env.example (modify)
  </files>
  <read_first>
    backend/models.py,
    frontend2/src/lib/utils.ts,
    frontend2/.env.example,
    frontend2/package.json
  </read_first>
  <action>
    Run `npm install zod` in the frontend2 directory to add Zod v4 as a production dependency (per D-09).

    Create src/lib/types.ts (per D-08): Define TypeScript interfaces mirroring every Pydantic model in backend/models.py. Include ALL models in the response chain — ResolvedAddress, ValuationRequest, MunicipioInfo, Listing, ValuationStats, MarketTransactionChartPoint, MarketTransaction, MarketTransactionsSummary, MarketTransactions, SearchStageResult, SearchMetadata, ValuationResponse. Use named exports, no default export. Follow the export pattern from src/lib/utils.ts. Map Pydantic Optional[T] = None to optional fields with `?` suffix and `| null` type. Map Python int/float to number, str to string, list[T] to T[]. These are zero-runtime-cost type definitions that prevent rework in downstream phases.

    Create src/lib/schemas.ts (per D-09): Import `z` from 'zod'. Export a `valuationRequestSchema` using z.object with these fields: address (z.string().min(1)), m2 (z.number().int().min(20).max(500)), bedrooms (z.number().int().min(1).max(10)), bathrooms (z.number().int().min(1).max(5)). Export the inferred type as `ValuationRequestForm` via z.infer. These are frontend UX bounds — intentionally stricter than the backend (e.g., m2 minimum 20 not 0, bedrooms minimum 1 not 0).

    Create src/lib/api.ts (per D-10): Import ValuationRequest and ValuationResponse types from ./types. Define API_BASE from import.meta.env.VITE_API_URL with fallback 'http://localhost:8001'. Export an async function valuateProperty(request: ValuationRequest): Promise<ValuationResponse> that POSTs to `${API_BASE}/api/valuation` with JSON body and Content-Type header. Throw an Error with status code on non-ok responses. Return res.json() typed as ValuationResponse. CRITICAL: the endpoint is /api/valuation (NOT /api/valuate — the CONTEXT.md D-10 has a typo, verified against backend/main.py line 89). Per D-11, keep this module thin — just the fetch wrapper, no retry logic or state management.

    Update frontend2/.env.example to contain VITE_API_URL=http://localhost:8001 (replacing the current Bright Data config which belongs in the backend, not the frontend).
  </action>
  <verify>
    <automated>cd frontend2 && node -e "require('./node_modules/zod/package.json').version" && test -f src/lib/types.ts && test -f src/lib/schemas.ts && test -f src/lib/api.ts && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists zod as a dependency (version ^4.x)
    - src/lib/types.ts exports interfaces: ResolvedAddress, ValuationRequest, MunicipioInfo, Listing, ValuationStats, MarketTransactionChartPoint, MarketTransaction, MarketTransactionsSummary, MarketTransactions, SearchStageResult, SearchMetadata, ValuationResponse
    - types.ts ValuationRequest has fields: address(string), m2(number), bedrooms(number), bathrooms(number), selected_address(ResolvedAddress | null optional)
    - types.ts ValuationResponse has fields: municipio(MunicipioInfo), listings(Listing[]), stats(ValuationStats), search_url(string), search_metadata(SearchMetadata), market_transactions(MarketTransactions | null optional)
    - src/lib/schemas.ts exports valuationRequestSchema and ValuationRequestForm type
    - schemas.ts address uses z.string().min(1), m2 uses z.number().int().min(20).max(500), bedrooms min(1).max(10), bathrooms min(1).max(5)
    - src/lib/api.ts exports valuateProperty function
    - api.ts contains `${API_BASE}/api/valuation` (not /api/valuate)
    - api.ts reads VITE_API_URL from import.meta.env with localhost:8001 fallback
    - .env.example contains VITE_API_URL=http://localhost:8001
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>Zod installed, all three foundation modules compile without errors, API client targets correct /api/valuation endpoint, .env.example documents VITE_API_URL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → backend API | API client defines the fetch target; no actual calls in Phase 1 |
| build-time env → bundle | VITE_API_URL is inlined at build time via import.meta.env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-01-01 | Tampering | src/lib/api.ts | accept | React auto-escapes JSX; API responses are typed. No user input processing in Phase 1. Phase 4 wires Zod schema for input validation. |
| T-01-02 | Information Disclosure | src/lib/api.ts | accept | VITE_API_URL is build-time only (Vite inlines it). No secrets in frontend env vars. Backend URL in production bundle is intentional (public API). |
| T-01-03 | Elevation of Privilege | package.json (zod dep) | accept | Zod is a well-maintained, widely-used validation library. Pinned to major version. Standard supply chain risk. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (all types resolve)
- No demo files remain in src/assets/ or src/components/ (TabBar, tabs, App.css gone)
- `grep -r "TabBar\|react\.svg\|vite\.svg\|hero\.png\|App\.css" frontend2/src/` returns no results
- index.html has lang="es" and Spanish title
</verification>

<success_criteria>
Foundation modules compile, Zod schema validates correct payloads and rejects invalid ones, API client targets the correct endpoint, demo artifacts fully removed, Spanish language declared.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-branded-shell/01-01-SUMMARY.md`
</output>
