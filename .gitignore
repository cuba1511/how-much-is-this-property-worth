.venv/
venv/
__pycache__/
*.pyc
.env
deploy/.env
.DS_Store
node_modules/
data/
# Figma token exports (reference only — tokens live in frontend/src/index.css)
design-tokens/*

# Evaluation harness — generated reports + response cache
tests/evaluation/reports/*
!tests/evaluation/reports/.gitkeep
tests/evaluation/.cache/

# SQLite databases (leads + valuations live here)
backend/data/
*.db
*.db-journal
*.db-wal
*.db-shm
# Vite pre-bundle cache (never commit)
frontend/.vite/
frontend2/

# Legacy folder name — removed in a08a7e2
